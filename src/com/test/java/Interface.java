package com.test.java;

public interface Interface {
	 
	int x = 10; 		//In interface, only final variables and methods etc are taken, as 'final' are only initialized not declared
	void m1();
}
